import { SectionHeader } from "@/components/common";

export function ProjectHeader() {
  return (
    <SectionHeader
      badge="Featured Project"
      title="Built for real users, not just for my resume."
      description="Every project focuses on solving real-world problems through scalable architecture, clean code and modern development practices."
    />
  );
}
